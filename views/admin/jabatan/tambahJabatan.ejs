<%- include('../partials/topTemplate') %>

<!-- Begin Page Content -->
<div class="container-fluid">

  <!-- Page Heading -->
  <div class="d-sm-flex align-items-center justify-content-between mb-4">
      <h1 class="h1 mb-0 text-gray-800">Tambah Jabatan</h1>
  </div>

  <% if (req.session.error) { %>
    <% req.session.error.forEach(error => { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <%= error.msg %>
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
    <% }) %>
  <% } %>

  <% delete req.session.error; %>
  
  <!-- Content Row -->
  <div class="row">
    <div class="container-fluid">
      <div class="card">
        <div class="card-body">
          <div class="row">
            <div class="col-lg-6">
              <form method="post" action="<%= baseUrl %>/admin/jabatan/tambah">
                <div class="form-group mb-4">
                  <label for="nama" class="text-dark">Jabatan</label>
                  <input type="text" name="jabatan" class="form-control" value="<%= (req.session.old) ? req.session.old.nama : '' %>">
                </div>
                <div class="form-group mb-4">
                  <label for="username" class="text-dark">Username</label>
                  <input type="text" name="username" class="form-control" value="<%= (req.session.old) ? req.session.old.username : '' %>">
                </div>
                <div class="form-group mb-4">
                  <label for="password" class="text-dark">Password</label>
                  <input type="password" name="password" class="form-control">
                </div>
                <div class="form-group mb-4">
                  <label for="password_confirm" class="text-dark">Konfirmasi Password</label>
                  <input type="password" name="password_confirm" class="form-control">
                </div>
                <a href="<%= baseUrl %>/admin/user-admin/daftar" class="btn btn-danger">Batal</a>
                <button type="submit" class="btn btn-primary">Tambah</button>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

</div>
<!-- /.container-fluid -->

<%- include('../partials/bottomTemplate') %>